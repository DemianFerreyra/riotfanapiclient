export interface Filters {
    name: string;
    imageURL: string;
}
export interface Cards {
    name: string;
    id: Number;
    pngURL: string;
}
export interface ProductsState {
    characters: [],
};